# مراحل تحتاجك

بقيت واحدة فقط: المرحلة ٢. وما قبلها وما بعدها منجَز.

أُنجز البناء والاختبار محلياً. ما تبقّى كله يحتاج تسجيل دخول بحسابك، ولا يمكن لأحد غيرك فعله.

## ~~المرحلة ١ — رفع المستودع وتشغيل الصفحات~~ ✅ تمّت

المستودع: https://github.com/a541ana-droid/madda-board
الموقع الحيّ: https://a541ana-droid.github.io/madda-board/

يعمل الآن ويعرض «لم يُربط النظام بعد بالخادم» — وهذا هو السلوك الصحيح حتى تُنجز المرحلة ٢.

## المرحلة ٢ — مشروع Apps Script (يحتاج حسابك في Google)

اتبع خطوات التركيب في [README.md](README.md) بالترتيب: شيت ومجلد، لصق ملفات `apps-script/`،
تعبئة الخصائص، تشغيل `setup`، ثم النشرتين.

بعد نشرة القراءة، الصق رابطها في موضعين: خاصية `READ_EXEC_URL`، وسطر `const API`
في أول `docs/index.html`.

## المرحلة ٣ — مفتاح Gemini (اختياري)

بدونه يعمل النظام كاملاً، ويُعطَّل شيئان فقط: الفحص الآلي للمحتوى، ومساعد لوحة الإدارة.
المفتاح من `aistudio.google.com` ويوضع في خاصية `GEMINI_KEY` — ولا يُكتب في أي ملف هنا أبداً.

---

## ما تم التحقق منه فعلياً (لا افتراضاً)

شُغّلت صفحة الطلاب محلياً وفُحصت في المتصفح:

- العرض بالعربية من اليمين، في الوضعين الليلي والنهاري، وعلى مقاس الجوال.
- التصفية حسب النوع تعمل.
- «عرض السجل الكامل» يفتح النسخ السابقة بنصوصها القديمة — الشفافية تعمل كما هي مقصودة.
- مسار انقطاع الاتصال: العرض من النسخة المحفوظة مع شريط تنبيه يذكر تاريخ آخر تحديث.
- كل ملفات الكود اجتازت فحص الصياغة، ولا يوجد أي سر في المستودع.

**عيبان اكتُشفا بالتشغيل وأُصلحا:** شريط التنبيه كان بألوان ليلية فوق الوضع النهاري،
وعامل الخدمة كان يخدم نسخة قديمة من الصفحة بعد التحديث (صار يطلب الشبكة بلا تخزين وسيط).

ما لم يُختبر بالتشغيل: كود Apps Script نفسه — لا يعمل إلا داخل Google. رُوجع سطراً سطراً،
وسأتحقق منه عملياً بعد إتمامك للمرحلة ٢.

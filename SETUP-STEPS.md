# ما تبقّى: ثلاث خطوات (٥ دقائق)

المشروع **«لوحة إعلانات المواد»** جاهز في حسابك على script.google.com، والكود مرفوع فيه
(ملفان: `الرمز.gs` و`Panel.html`). ما يلي يحتاج موافقتك الشخصية، ولا يستطيع أحد فعله نيابةً عنك.

افتح المشروع في **Chrome عندك** (لا في متصفح داخل تطبيق، فهو يحجب نافذة الأذونات):
https://script.google.com/home

---

## ١) التهيئة — ضغطة واحدة تُنشئ كل شيء

في شريط المحرر: اختر من القائمة المنسدلة الدالة **`bootstrap`** ثم اضغط **تنفيذ**.

ستظهر شاشة الأذونات:
- **مراجعة الأذونات** ← اختر حسابك
- تظهر «لم يتم إثبات ملكية التطبيق» ← **خيارات متقدمة** ← **الانتقال إلى لوحة إعلانات المواد (غير آمن)**
  (هذا طبيعي: التطبيق كتبتَه أنت لنفسك ولم يمر بمراجعة Google، والأذونات تصل إلى ملفاتك أنت فقط)
- **السماح**

تُنشئ الدالة تلقائياً: الشيت، ومجلد الملفات، والمفتاح الداخلي، وتضبط بريدك كمدير.
سترى في السجل أسطراً تؤكد ذلك.

## ٢) نشرة القراءة (الطلاب)

**نشر ← نشر جديد ← ⚙ اختيار النوع ← تطبيق ويب**

| الحقل | القيمة |
|---|---|
| التنفيذ بصفتي | **أنا** (بريدك) |
| مَن لديه إمكانية الوصول | **أي شخص** |

اضغط **نشر** ← انسخ **رابط تطبيق الويب**.

ثم ضع هذا الرابط في مكانه داخل الإعدادات:
**⚙ إعدادات المشروع ← خصائص النص البرمجي ← إضافة خاصية**
- الاسم: `READ_EXEC_URL`
- القيمة: الرابط الذي نسخته

## ٣) نشرة اللوحة (أنت والمحررون)

**نشر ← نشر جديد ← تطبيق ويب** مرة أخرى، وهذه المرة:

| الحقل | القيمة |
|---|---|
| التنفيذ بصفتي | **المستخدم الذي يصل إلى تطبيق الويب** |
| مَن لديه إمكانية الوصول | **أي شخص لديه حساب على Google** |

انسخ الرابط — هذا هو رابط لوحتك ولوحة المحررين.

---

## أرسل لي الرابطين

أضع رابط القراءة في `docs/index.html` وأدفعه إلى GitHub، ثم أختبر النظام كاملاً:
إنشاء مادة، ودعوة محرر، ونشر إدخال، وتعديله للتأكد من ظهور النسختين في السجل العام.

## اختياري: مفتاح Gemini

من `aistudio.google.com` ← أضفه خاصيةً باسم `GEMINI_KEY` بنفس طريقة الخطوة ٢.
بدونه يعمل كل شيء عدا الفحص الآلي للمحتوى ومساعد لوحة الإدارة.
